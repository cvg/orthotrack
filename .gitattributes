# Optional: enable Git LFS for the README wordmark if you prefer not to
# store the ~5MB GIF as a normal blob. Left unset by default for simpler clones.
# assets/orthotrack_wordmark_transparent.gif filter=lfs diff=lfs merge=lfs -text
