# OrthoTrack Requirements

# Core
# Keep NumPy 1.x / OpenCV 4.x with Open3D 0.18 for create_movingdrone --render.
numpy>=1.26.0,<2
opencv-python>=4.10.0.84,<5
scipy>=1.14.0
pillow>=10.4.0
tqdm>=4.66.5
einops>=0.8.0
matplotlib>=3.9.2
rasterio>=1.3.10
pyproj>=3.6.1
pandas>=2.0.0

# Deep Learning
torch>=2.4.0
torchvision>=0.19.0

# Video decoding (optional, falls back to OpenCV)
decord>=0.6.0

# Logging (create_movingdrone + tests)
loguru>=0.7.0

# LoD mesh (building geometry) + mesh RGB/depth render (--render)
trimesh>=4.0
PyOpenGL>=3.1
# Required for mesh RGB/depth render (--render); keep with NumPy/OpenCV pins above.
open3d==0.18.0

# RoMaV2 dependencies
kornia>=0.7.3
fused-local-corr; sys_platform == 'linux'
